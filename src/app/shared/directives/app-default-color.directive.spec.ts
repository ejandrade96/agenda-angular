import { AppDefaultColorDirective } from './app-default-color.directive';

describe('AppDefaultColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDefaultColorDirective();
    expect(directive).toBeTruthy();
  });
});
